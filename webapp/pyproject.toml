[project]
name = "aap-appliance-webapp"
version = "0.1.0"

[project.optional-dependencies]
dev = ["pytest>=8", "pyyaml>=6", "httpx>=0.27", "fastapi>=0.115", "python-multipart>=0.0.9"]

[tool.pytest.ini_options]
testpaths = ["tests"]
